import { EnumSelectPipe } from './enum-select.pipe';

describe('EnumSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
